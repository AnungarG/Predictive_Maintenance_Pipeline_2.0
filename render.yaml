services:
  - type: web
    name: nlng-predictive-maintenance-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn idss.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONPATH
        value: .