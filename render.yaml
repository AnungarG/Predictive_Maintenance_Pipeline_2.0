services:
  - type: web
    name: nlng-predictive-maintenance-api
    env: python
    rootDir: idss/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT