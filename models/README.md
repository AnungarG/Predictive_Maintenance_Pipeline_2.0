# Model artefacts

Place approved trained artefacts here for deployment. Do not commit large raw datasets or model binaries to a public GitHub repository unless policy permits it.

Expected deployment artefacts:
- classification/dl_classifier_mlp.keras
- regression/gradient_boosting_regressor.pkl
- preprocessing/dl_feature_imputer.pkl
- preprocessing/dl_feature_scaler.pkl
